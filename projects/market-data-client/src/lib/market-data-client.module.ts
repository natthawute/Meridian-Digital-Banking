import { NgModule } from '@angular/core';

/**
 * Market data client module. The `MarketDataService` is provided in `root`
 * so it can be consumed by any banking app.
 */
@NgModule({})
export class MarketDataClientModule { }
