import { NgModule } from '@angular/core';

/**
 * Analytics SDK module. The `AnalyticsService` is provided in `root` so
 * it can be consumed anywhere in the workspace.
 */
@NgModule({})
export class AnalyticsSdkModule { }
